[workspace]
members = [
    ".",
    "crates/lingo_runtime",
]
exclude = [
    "target/lingo_run",
]

[package]
name = "lingo"
version = "0.1.0"
edition = "2021"

[dependencies]
lingo_runtime = { path = "crates/lingo_runtime" }
clap = { version = "4.5", features = ["derive"] }

